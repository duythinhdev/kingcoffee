export enum OrderStatusDescription{
    ordered = 'order is successful',
    confirmed = 'order is confirmed',
    canceled = 'order is canceled',
    processing = 'order is processing',
    packed = 'order is packed',
    handedOver = 'order is handedOver',
    shipping = 'order is shipping',
    successDelivered = 'order is successDelivered',
    failOrdered = 'order is failed',
    failDelivered = 'delivery is failed',
}